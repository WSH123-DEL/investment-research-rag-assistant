# 具身智能研报问答助手（LangChain 1.x）
# 安装：python -m pip install -r requirements.txt

# LangChain 核心与当前代码仍使用的经典 RAG 链
langchain>=1.3,<2
langchain-core>=1.5,<2
langchain-classic>=1.0,<2

# 独立集成包：DeepSeek / Chroma / Hugging Face / 文本切分
langchain-deepseek>=1.1,<2
langchain-chroma>=1.1,<2
langchain-huggingface>=1.2,<2
langchain-text-splitters>=1.1,<2

# PDF、向量库、嵌入模型与网页界面
pypdf>=6.14,<7
chromadb>=1.5,<2
# Chroma 的 OTLP 导出器需与当前 protobuf/opentelemetry SDK 同代，避免旧生成代码报错。
opentelemetry-exporter-otlp-proto-grpc>=1.44,<2
sentence-transformers>=5.6,<6
gradio>=6.20,<7
python-dotenv>=1.2,<2

# 注意：不再直接依赖已停止维护的 langchain-community。
# langchain-deepseek 会按兼容范围传递安装 langchain-openai/openai。
